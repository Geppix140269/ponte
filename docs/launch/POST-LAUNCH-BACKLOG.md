# Ponte Trade Post-Launch Backlog

**Mode:** Launch Mode is active until the repository owner explicitly closes it.

This is the canonical index of useful work that does not prevent safe launch. Detailed implementation discussion may live in GitHub issues, but every deferred item discovered during Launch Mode must also be indexed here.

Post-launch work must not be implemented during an unrelated launch task unless the repository owner explicitly promotes it to a Launch Blocker or separately authorises it.

## Open tickets

| ID | Title | Category | Discovered | Source task or audit | Problem and evidence | Recommended action | Risk if deferred | GitHub issue | Status |
|---|---|---|---|---|---|---|---|---|---|
| PL-001 | Supabase Preview integration points to an unavailable project | DevOps | 2026-07-28 | Production migration reconciliation | Preview check is misconfigured and permanently red; it is not a production gate | Disable until an owned non-production project is reproducible | Low for launch; creates misleading CI noise | #87 | Open |
| PL-002 | Reproducible non-production database baseline | Infrastructure | 2026-07-28 | Production migration reconciliation | Repository cannot currently reproduce all retained production schema dependencies | Classify and baseline retained objects without a wholesale production dump | Medium; local development remains constrained | #84 | Open |
| PL-003 | Retire or replace broken `match_hs_codes` function | Data architecture | 2026-07-28 | Schema baseline classification | Function references absent objects and is unused | Remove through a separately reviewed migration after launch | Low; dead production object remains | — | Open |
| PL-006 | Orphan private storage bucket `ponte-deal-docs` | Data architecture | 2026-07-29 | Deal Room Gate A preflight (#97) | Private bucket created 2026-06-05, before any accepted Deal Room authority. Zero objects, zero `storage.objects` policies naming it, zero code references. Closed rather than exposed: with no policy only the service role can reach it | Classify and remove through a separately reviewed change after launch. The Deal Room slice deliberately creates a new, precisely named bucket rather than inheriting an object of undocumented intent | Low; a dead private bucket remains | — | Open |
| PL-007 | `verification-docs` bucket has no storage policy | Security review | 2026-07-29 | Deal Room Gate A preflight (#97) | No `storage.objects` policy names the bucket; it is reached only through `createAdminClient()` (service role) in `app/[locale]/admin/verifications/page.tsx`. That is consistent and not fail-open, but whether a member-facing upload or read path was ever intended is unresolved | Decide the intended access model and either record the service-role-only design or add explicit policies | Low; no exposure. Risk is a future member-facing feature silently failing or being given the wrong access model | — | Open |
| PL-008 | The organisation layer is unpopulated | Data architecture | 2026-07-29 | Deal Room Gate A preflight (#97) | Production holds 0 rows in `organizations` and 0 of 9 profiles carry `organization_id`, yet the Deal Room permission model and Business Passport are organisation-aware | Design organisation creation, ownership and membership as a first-class capability. The Deal Room slice handles its own need at admission time only, through organisation-or-declared-capacity | Medium; every organisation-scoped capability after the Deal Room slice depends on it | — | Open |
| PL-009 | Stale Deal Room authority and status records | Documentation | 2026-07-29 | Deal Room Gate A preflight (#97) | `CURRENT-STATE.md` cites `ADR-0008-deal-room-product-contract.md`, which does not exist (the contract is ADR-0003; ADR-0008 is the detailed definition). ADR-0009 names `PT-TECH-2026-07-27-01-...` as its related authority; that file is absent from the repository. Several workstreams are still described as "on branch" although they are now on `main` | Correct the cross-references. Decide whether the missing PT-TECH authority is to be written or ADR-0009 amended to stand alone | Low; misleads the next agent about what is decided and what is implemented | — | Open |
| PL-010 | Disposition of the legacy Deal-era database cluster | Data architecture | 2026-07-29 | Deal Room Gate A preflight (#97) | `deals`, `deal_documents`, `deal_events`, `deal_status_history`, `messages`, `settlements`, `settlement_milestones`, `settlement_events` and `is_deal_participant()`: all empty, referenced by no code, superseded in function by the `deal_room_*` design. Six of the eight tables have no write policy at all | Decide retirement or retention **after** the Deal Room slice is production-verified, one object at a time, child before parent. Issue #97 explicitly excludes touching them now | Low; empty, closed, unreferenced objects remain | — | Open |
| PL-011 | Immutable multi-version archive for Deal Room agreement documents | Data architecture | 2026-07-29 | Deal Room owner final trust review (#97, PR #98) | `deal_room_agreement_documents` is keyed on `kind` alone, so it holds exactly one row per agreement and its `current` flag cannot distinguish a live document from a retired one. Admission joins acceptances to that row on `version` and `sha256`, so today an acceptance can only be satisfied by the canonical v1 — but publishing a v2 would have to overwrite v1 in place, and every acceptance recorded against v1 would then be unverifiable against the row that authorised it. This does not block the first launch: v1 is reproducible from the shipped source in `lib/deal-room/agreements.ts` and its checksum is asserted against the migration on every test run | Re-key on `(kind, version)`, make published rows immutable by trigger, and give admission an explicit rule about which versions remain acceptable while a participant is mid-admission. Do it **before any v2 agreement is published**, not before launch | Medium if a second version is ever published; none while v1 is the only document | — | Open |
| PL-012 | An invitation that lapses unopened is never recorded as expired | Data architecture | 2026-07-29 | Deal Room Gate B technical approval (#97, PR #98) | `deal_room_invitations.state` only becomes `expired` inside `deal_room_accept_invitation()`, on the attempt that finds `expires_at < now()`. Nothing else moves it. Since acceptance is now bound to the intended identity, only the intended principal can even reach that line, so an invitation nobody opens stays `sent` in the durable row indefinitely, its workspace stays `invitation_pending` and its room stays `awaiting_principal_admission`. This is not an access defect: the command still refuses the lapsed token on its own `expires_at`, and the invitee-facing resolver in `lib/deal-room/invitation.ts` compares the timestamp as well as the state, so nothing expired is ever honoured or shown as live. What is wrong is the durable record - it states an invitation is outstanding when it is not, and Ponte reads its own history as fact | Give expiry a writer that does not depend on somebody arriving: a scheduled sweep, or derive the reported state from `expires_at` at read time in the database rather than only in TypeScript. Decide at the same time whether the workspace and room should fall back, and whether a lapsed invitation may be reissued or must be re-proposed | Low for access, medium for reporting; a room register and any future activity or entitlement metric will overstate live invitations | — | Open |
| PL-013 | Legacy classification keys are not reconciled on resume | Data integrity | 2026-07-29 | ADR-0014 follow-up, resume round-trip verification | `canonicalServiceCategory`, `canonicalPartnerType` and `canonicalRelationshipTerm` exist in `lib/taxonomy/` to map a superseded stored key onto its current one, and nothing calls them. `draftFromRow` reads `service_category_key` raw, so a record stored under a superseded key resumes with an unrecognised category; `clearForeignClassification` then drops its specialisations at the submit boundary, silently, on edit. Demonstrated by the round-trip tests in `lib/structure/__tests__/resume.test.ts` section E, which had to use current keys for this reason | Call the canonical resolvers in `draftFromRow`, and audit production for rows carrying superseded keys before deciding whether a backfill is also needed | Medium. Confined to editing a record stored under a superseded key; production incidence is unmeasured, so this must not be reported as zero. A new record cannot reach the state, because the composer only ever writes current keys | — | Open |
| PL-014 | Generalise listing reads to drop individually named unavailable optional columns | Resilience | 2026-07-29 | ADR-0014 production-state reconciliation | The three listing readers name every column they want in one select, and PostgREST refuses a select naming an absent column outright: nothing partial comes back, an error does. While `20260728c` and `20260728e` were unapplied, that meant `/marketplace/l/[ref]` answered `notFound()`, `/find/o/[ref]` answered `missing`, and `/opportunities` told a member they had no records. **Both migrations are now applied, so no column those readers name is absent and the exposure is closed** - this is resilience against the NEXT unapplied migration, not a live fault. | Adopt the tested implementation preserved at PR #101 head `53c9d99`: `lib/listings/read-fallback.ts` reads the column out of the error through the existing `missingColumnFrom`, drops that one, and retries until the select is acceptable or the error is not a missing column. Essential columns are never dropped - `id`, `user_id`, `ref`, `type`, `product`, `details`, `status`, `validity_type`, `valid_until`, `market_family`, `market_intent` - because losing the last two does not omit a fact, it inverts one. 25 tests, including the sequential multi-column case and per-reader cases that parse each reader's real column list out of its source. **Not implemented in the reconciliation PR that recorded this ticket** | Low now that the columns exist. Medium the next time a migration ships ahead of its application, which is the normal order in this repository | — | Open |
| PL-015 | The `20260722b` ledger row holds a placeholder where its SHA-256 should be | Data architecture | 2026-07-29 | Deal Room Gate C production preflight (#97, PR #105) | `public.schema_migrations` records `20260722b_hs_codes.sql` with the literal string `applied-via-management-api` in its `sha256` column instead of a digest. One row of 43, predating all current work. Every other row carries a real hash, and after PR #106 every one of those matches its file byte for byte. The row is therefore the single exception to the invariant `GATE-C-TEST-PLAN.md` §4.8 asserts, and an audit that finds it without context could read it as a fresh mismatch | Compute the SHA-256 of the file as it stands, satisfy yourself it is the version production ran, and record it - or, if that cannot be established, replace the placeholder with a string that says so explicitly rather than one that looks like a method note. Not repaired in PR #105 by owner decision: it is not a Deal Room blocker | Low. Nothing depends on it; the cost is one unverifiable row in an otherwise complete ledger | — | Open |
| PL-016 | Apply the Market Signals search indexes, then decide on full-text | Performance | 2026-07-29 | LB-007, Market Signals search | The search runs as `ilike` over eight public columns, which no btree can serve, so Postgres plans a sequential scan. **Measured on 30 July 2026 against production with no index applied** (`npx tsx scripts/verify-signal-search.ts`, 3,458 eligible): wall-clock round trips of 198 to 1,037 ms across two runs, the slowest being a five-variant alias group across nine columns on a cold connection. That includes network latency and cannot be decomposed client-side, so it is an upper bound on the database work. An earlier estimate in this row said single-digit milliseconds; that was reasoned from the row count and never measured. It ships at this cost because the alternative was a search that returned nothing until somebody hand-applied a migration, since a merge applies no SQL in this repository. The cost is linear, so it is a real problem at 100,000 records and not at 3,500. `supabase/migrations/20260730a_market_signal_search.sql` is written, additive, index-only and **not applied**: it creates `pg_trgm` and eight partial GIN trigram indexes, which is the one thing that makes an unanchored `ILIKE '%...%'` indexable, plus `(spotted_at desc, id desc)` for the paging order. Applying it changes no result, no count and no row | Apply the migration with owner approval and record it in `DATABASE-STATE.md`. Then measure: if trigram matching is not enough at the next inventory size, move to a maintained `tsvector` search document with a GIN index, which needs a generated column, a backfill over every row and an RPC, because PostgREST's filter grammar cannot express `@@` | Low now, high at an order of magnitude more inventory. The failure mode is latency, not a wrong answer | - | Open |
| PL-017 | Free-text search cannot reach the canonical taxonomy, because nothing carries it | Data architecture | 2026-07-29 | LB-007, Market Signals search | Search reads the public text columns. It deliberately does not read `service_category_key`, `product_sector_key`, `distribution_partner_type_key`, `service_subcategory_keys` or `territory_codes`, because **zero of the 3,491 eligible signals carries any of them** (`DATABASE-STATE.md`, 28 July: applying `20260728a` created the columns and classified nothing). Searching an entirely null column would add query weight and predicate length for no possible match, and the two array columns cannot be matched with `ilike` at all. So a search for `ocean freight` today finds records whose prose says it, and would not find a record classified `freight.ocean` whose prose says `sea shipping`. The alias vocabulary closes part of that gap by widening the phrases; it is not a substitute for classification | Classify the existing inventory (ADR-0011 section 5.2, still not done), then extend the search to the canonical keys and add the taxonomy-label-to-key mapping. Until then the structured filters remain the instrument for canonical keys, and they already report `nothing_classified` truthfully rather than returning an empty list | Medium. Not a wrong answer - the coverage states already tell a member when a filter can see only part of the board - but the inventory is less findable than its classification columns imply | - | Open |
| PL-018 | The approximately 160-signal batch is still unreconciled | Data integrity | 2026-07-29 | LB-007 scope review | ADR-0011 section 6 requires the supplied batch to be located and reconciled with exact source, imported, duplicate, rejected, quarantined, private and public-approved counts. It has not been. Recorded here rather than in the search work, which was explicitly scoped to make the existing inventory findable and explicitly forbidden from backfilling classifications or importing records | Run the existing idempotent import against the batch and publish the reconciliation table ADR-0011 specifies | Low for search correctness; the board's totals are already derived from the database rather than hard-coded, so they stay truthful whatever the batch adds | - | Open |
| PL-019 | Free-text search cannot fold accents at the database | Data architecture | 2026-07-30 | LB-007 acceptance audit, correction 3 | `ILIKE` folds case and never accents, and PostgREST's filter grammar cannot call `unaccent`, so the accent handling is asymmetric and is documented as such rather than claimed complete. An accented query searches BOTH spellings and therefore reaches an accented stored value (verified live on 30 July 2026: `café` searched as `[café` or `cafe]`, one match). An UNACCENTED query reaches only the unaccented spelling, because generating every accented spelling of a word is combinatorial. The in-memory matcher used by the fixture gallery folds both sides and is therefore MORE permissive than production, which is asserted in `lib/search/__tests__/search-scope.test.ts` so the difference cannot be mistaken for parity | Fold at the database: a stored generated column carrying `unaccent(lower(...))` of the public text, with its own trigram index, searched instead of the raw columns. That needs a column, a backfill over every row and therefore an owner-approved migration, which is why it is not in the launch change | Low. A member searching `cafe` for a record stored as `Café` finds nothing and has no way to know why. Confined to accented stored values, which in this inventory are rare: source product names are predominantly English | - | Open |
| PL-020 | Classify the existing Market Signals inventory, then source genuine Trade services and Distribution inventory | Inventory | 2026-07-30 | LB-007 filter-truthfulness correction; family counts executed 30 July 2026 | **Measured, not inferred** (`npx tsx scripts/verify-signal-search.ts`, 30 July 2026, read-only against production PostgREST; evidence `docs/codex/audits/market-signals-search/2026-07-30-postgrest-verification.txt`): **3,458 eligible signals overall, 0 canonically classified as Products, 0 as Trade services, 0 as Distribution.** Not one eligible record carries a canonical `market_family`. An earlier version of this row said products 3,458, which was an inference from what the records ARE rather than a read of the column that decides what the filter can do, and it is corrected here. The distinction matters commercially: the existing records are **semantically product-oriented** - product names, HS codes, origins, destinations - and they are live, public and reachable by search today. They are simply **not available to the canonical family filter**, because that filter reads `market_family` and nothing else. So this is TWO gaps wearing one number, and they need different work: **(a) classification** - the product inventory exists but is unclassified, so even the Products filter cannot be offered; **(b) inventory** - no Trade services or Distribution record exists at all, so no classification pass can produce one. The family selector is therefore absent because no family is available, not because one is: `showFamilySelector` needs two available families and today it has none. Neither gap is an interface defect, and the interface is already truthful about both | Two pieces of work, in this order, both owner-approved. **(a) Classify the existing product inventory.** Set `market_family` on the records that genuinely are product signals, per record and evidenced, through the existing import and classification path rather than a blanket `UPDATE` that would assert a classification nobody checked. Record the reconciliation - how many rows examined, classified, left unclassified and why - the same way ADR-0011 section 6 requires for an import. Once the count is non-zero the Products controls appear on their own, with no code change. This is `market_family` specifically; the category axes below it (`product_sector_key` and the rest) are PL-017, and the same classification pass should carry both so they do not diverge. **(b) Source genuine Trade services and Distribution inventory.** **Trade services:** freight forwarding, customs clearance, inspection and certification, warehousing, cold-chain logistics, trade finance, cargo insurance, shipping and transport, compliance support. **Distribution:** distributor wanted, importer wanted, commercial agent wanted, sales representative wanted, exclusive and non-exclusive representation, regional distribution mandates, channel-partner opportunities. **Integrity conditions, all binding on both pieces:** every record based on a real public signal or a genuine member submission; nothing fabricated; **no product buyer requirement relabelled as a service or distribution record**, which would make the filters appear populated while making every count and every match a lie; source attribution preserved; correct canonical family and category keys; expiry and public eligibility validated; the existing dedupe key enforced so a re-run imports nothing twice; classification confidence and provenance recorded where the schema permits | Medium, and commercial rather than technical. Ponte presents three equal market families and the family filter can currently answer for none of them, so the accepted market model has no classified inventory behind it at all. Nothing on the board is untruthful while the interface reflects the measurement, which is what the correction secured: the records are findable by search, and the page says only that the filtering is unavailable | - | Open |
| PL-021 | `/favicon.ico` returns 500 (app/ and public/ both claim it) | DevOps | 2026-07-30 | Platform UX audit (`docs/codex/audits/2026-07-30-platform-ux-audit/`) | `app/favicon.ico` and `public/favicon.ico` are byte-identical (15086 bytes). Next 14 App Router generates the `/favicon.ico` route from `app/`, and the static file at the same path collides, so `GET /favicon.ico` answers 500 on the dev server. Pre-existing on `main`, not caused by the audit branch or env. Browsers fall back to no icon; no journey is affected | Remove `public/favicon.ico` (keep the App Router `app/favicon.ico`), or vice versa, through a small separately reviewed change | Low; a broken tab icon and a noisy 500 in logs | — | Open |
| PL-022 | `/learn` index 404s | UX | 2026-07-30 | Platform UX audit | The `/learn` segment has leaves (`/learn/duties`, `/learn/trade-data`) but no index page, so a typed `/learn` 404s. Nothing in the codebase links to bare `/learn`, so it is unreachable through the UI | Add a `/learn` index that lists the two guides, or a redirect to the more general one | Low; only a manually typed URL is affected | — | Open |
| PL-023 | `/contact` is a `mailto:` link, not a tracked form | UX | 2026-07-30 | Platform UX audit | The contact route opens the visitor's mail client to `hello@ponte.trade` rather than submitting a form Ponte records and confirms. It works and is unambiguous, but there is no delivery confirmation and no logged record on the Ponte side, unlike "Ask Ponte to investigate" | Decide whether contact should become a recorded form with a success state and a desk notification, consistent with the other contact-Ponte CTAs | Low; the mailto works today | — | Open |
| PL-024 | Legacy intent picker does not switch product-intake copy on `/structure` | UX | 2026-07-30 | Platform UX audit | On `/structure` reached without a family entrance, choosing "Source a product" sets the legacy `intent` but not `canonical.intent`, which is what `ProductIntake` reads to pick its supply-vs-need copy. So the intake keeps the default "Tell Ponte what you supply" wording after "Source a product" is chosen. Cosmetic only: the resolver, payload and family are unaffected, and the entrance-driven journeys (which set `canonical`) are correct | Set a canonical pair from the legacy picker, or read the legacy `intent` in the intake copy selector | Low; copy mismatch on one entry path | — | Open |
| PL-031 | Wire the shared navigation guard and labelled Back across the remaining journeys | UX / accessibility | 2026-07-30 | Persistent navigation and unsaved-work brief; `docs/codex/audits/navigation-unsaved-work/AUDIT-2026-07-30.md` | Slice 1 delivered the shared foundation (`JourneyBack`, `UnsavedChangesDialog`, `useUnsavedGuard`, `lib/nav/dirty.ts`), replaced the two icon-only Back controls (`StructureComposer`, `CheckComposer`), guarded the Structure exit-to-home, and added branded error boundaries. **Owner-authorised full brief; the remaining surfaces are the enumerated follow-up, not deferred against the owner's wishes** — recorded here so the tail is visible in one place per Launch Mode. Remaining: guard and (where drafts persist) wire the three-action dialog into `marketplace/new`, `find/o/[ref]` (also an inert breadcrumb), Deal Room `propose` / `admission` / `blockers` / evidence-clarification, and `admin/listings` / `admin/verifications`; add the two missing Deal Room "Back to the room" controls (`[roomId]/activity`, workspace hub); fix `/learn/*` double `<main>` and the `not-found.tsx` second-CTA label/target; capture desktop + 390x844 + screen-reader evidence per wired journey | Continue through the ExecPlan `docs/plans/active/persistent-navigation-and-unsaved-work.md`, one journey per reviewable change with its own evidence, per the Constitution's delivery discipline | Low-to-medium: heavy free-text forms (Deal Room admission/authority, admin decision notes) can still silently lose typed input on an accidental navigation until wired | — | Open |

| PL-025 | A genuine notification-preferences page and model (the dead footer link that stood in for one is removed) | Feature | 2026-07-30 | Authentication and transactional email audit (LB-012) | **The dead link is fixed; the feature it implied is deferred.** Originally: `lib/email/shell.ts` linked "Notification preferences" in every application email footer to `route("/account/notifications")`, a route nothing under `app/` serves, so all thirteen templates shipped a 404. **Corrected on the LB-012 branch as the minimum safe fix:** the "Notification preferences" link is removed from the member footer, in HTML and plain text; Privacy and Terms, which both resolve, stay; no route was invented and no consent semantics changed — there was never a page behind the link to change. Asserted by test (`no application email links the non-existent /account/notifications route`, and `application email footers keep Privacy and Terms, which resolve`) and shown in the regenerated evidence `docs/codex/audits/email/evidence/listing-needs-information--gmail-app-non-google-account.png` (footer now reads "Privacy · Terms"). What remains is the real feature: a member has no way to manage what Ponte emails them | Build `/account/notifications` with an actual preference model — which categories a member may mute, honoured at send time in `lib/email/send.ts` — then restore the footer link pointing at it. It is a feature, not a link edit: a preferences page implies a stored preference and a send-time check, and `NotificationReason` in `lib/email/shell.ts` is the natural axis for it | Low. The dead link is gone, so nothing 404s and no member is misled; the absence of a preferences page is a missing capability, not a broken one | — | Open (dead link removed; feature deferred) |
| PL-026 | Authentication email has no plain-text part and no Reply-To, because Supabase SMTP has neither | Architecture | 2026-07-30 | Authentication and transactional email audit (LB-012) | Supabase's custom SMTP sends the HTML body only and exposes no `Reply-To` field. So the one Ponte email a member receives before anything else is the one place where "every email has a text alternative" cannot be met, and a reply to a sign-in code goes nowhere unless `auth@ponte.trade` forwards to `hello@ponte.trade`. `supabase/templates/auth-otp.txt` is generated so the wording is reviewable, and is never delivered. First assessed in `docs/platform/AUTH-EMAIL-SETUP.md` §3 on 22 July 2026 and reaffirmed here | Replace SMTP with a Supabase Auth send-email hook that calls the Resend API directly, where `reply_to` and a text part are both supported. That also brings the authentication email inside `lib/email/send.ts`, so it would be observable in the same place as everything else. §3's assessment stands: real work, not launch week | Low. The forward mitigates the reply path entirely and costs one mailbox rule; the missing text part affects a small minority of readers | — | Open |
| PL-027 | Three Supabase Auth templates are left as provider defaults | Documentation | 2026-07-30 | Authentication and transactional email audit (LB-012) | `Reset Password`, `Change Email Address` and `Invite user` are not generated by `lib/email/auth-templates.ts` and remain Supabase defaults. This is deliberate under ADR-0017 §10: Ponte has no password, and there is no invitation or email-change journey in launch scope. A template pasted for a journey that does not exist is one nobody will notice has gone stale. Recorded so the omission is a decision on the record rather than a gap somebody rediscovers | Generate each one at the point its journey is built, from the same shell, and add it to `supabase/templates/` with a checksum. `Change Email Address` is the likeliest first, and it carries a real security sentence the default does not | Low while none of the three journeys exists. It becomes a blocker for whichever journey ships first, because that journey's email would arrive as a Supabase default | — | Open |
| PL-028 | The "Ask Ponte about this" email link points at `/support`, which does not exist | Journey defect | 2026-07-30 | Authentication and transactional email audit (LB-012) | Found while fixing the `/account/notifications` footer link (PL-025), and **left unfixed on purpose — out of the LB-012 scope, which the owner defined as the footer link only.** `lib/email/templates.ts:400` (`listing_suspended`) and `:429` (`listing_flagged_member`) both emit `{ kind: "link", label: "Ask Ponte about this", href: route("/support") }`. Nothing under `app/[locale]/` serves `support`, there is no redirect in `next.config.mjs`, `middleware.ts` or `netlify.toml`, and no catch-all — so the link 404s. Same class as PL-025 but a body link on two member templates rather than the shared footer, and it is the one contextual action a member reads when their listing was suspended or flagged, so a dead link there is worse than in a footer | Point it at `/contact`, which exists (`app/[locale]/contact/page.tsx`) and is the real "ask Ponte" surface, or build `/support` if a distinct support surface is wanted. A one-line change per call site once the target is decided | Medium for those two templates: the link is the member's only offered response to a suspension or flag, and it fails. Low across the system: only two templates carry it | — | Open |

| PL-029 | Transactional emails still link members to the legacy `/marketplace` board | Journey / design | 2026-07-30 | LB-013 legacy-editor quarantine | Five member-facing templates in `lib/email/templates.ts` still route to `route("/marketplace")`: `listing_published` ("Manage or withdraw this listing"), `listing_flagged_member` ("Open your listing"), `listing_expiring` ("Let it expire"), `connection_requested` ("Review the request") and `connection_accepted` ("Open in Ponte"). `/marketplace` renders in the legacy obsidian chrome (`components/ChromeGate.tsx` does not bar it). It was **deliberately not repointed in LB-013**: `docs/codex/audits/constitution-rebuild/MARKETPLACE-DEPENDENCY-FINDING.md` established that the board carries three unique member functions with no home elsewhere — the owner-side connection decision, listing reconfirmation and the account brief — and the retire-or-rebuild question is escalated to the owner and undecided, so there is no approved constitutional destination to send these links to (a stop condition for this blocker). LB-013 fixed only the destination that had one (the editor → `/structure`) | Answer the `/marketplace` product question in the dependency finding (option 2, "redistribute then retire", is the strongest on the evidence), then repoint these five email links to the surface that inherits each function | Medium. Every one of these emails still lands a member in the deprecated application; the editor path, which was the acute failure, no longer does | — | Open |
| PL-030 | Remove the now-unreachable legacy `ListingForm` composer | Dead code | 2026-07-30 | LB-013 legacy-editor quarantine | With `/marketplace/new` quarantined to a redirect, `components/ListingForm.tsx` is rendered by nothing. It remains in the tree only because deleting it is broader than LB-013's minimum, and it is the single entry pinned in the `check-governance.mjs` LB-013 route-audit baseline (its own internal `/login?next=/marketplace/new...` redirect still names the retired path). Confirm no other importer exists (`lib/listing-terms.ts` and the `/api/marketplace/*` handlers are the API, not this component), delete the component and its styles, and remove the baseline entry so the ratchet tightens to zero. Note `components/ListingForm.tsx` also appears in the `check-governance.mjs` LUCIDE and RAW_SVG baselines — remove it from those in the same change | Delete `components/ListingForm.tsx` (and any now-orphaned imports/styles), then drop it from the three `check-governance.mjs` baselines | Low. Unreachable dead code; no member can reach it and no generated link points at it | — | Open |

## Completed tickets

Move completed items here and preserve the original ID.

| ID | Title | Completed | Resolution PR or issue | Verification |
|---|---|---|---|---|
| PL-004 | Duplicate migration identifier `20260728d` made `npm run verify` fail on `main` | 2026-07-29 | Branch `agent/deal-room-launch-slice`, issue #97 decision 3 | `20260728d_family_commercial_terms.sql` renamed to `20260728e_`. The **applied** `20260728d_verification_level_canonical.sql` keeps its identity, because it is recorded in `public.schema_migrations` under that exact name with its SHA-256; renaming it would have invalidated the ledger record. The SQL inside the renamed file is unchanged. `node scripts/check-migrations.mjs` now reports `ok 46 migrations: no duplicate dated identifiers`. References updated in `OPERATIONS_LOG.md`, `DATABASE-STATE.md` and `CURRENT-STATE.md`. **Independently re-indexed while this repair was in flight:** PR #102 opened a second row for the same defect, numbered PL-005, which the Completed table was already using for the `check-launch-mode` repair. That row is removed rather than renumbered - the condition it describes no longer exists, and this entry is its evidence. Recorded here rather than deleted silently, per this file's own rule. |
| PL-005 | `check-launch-mode.mjs` failed on a line-wrapped sentence in `AGENTS.md` | 2026-07-29 | Branch `agent/deal-room-launch-slice`, issue #97 decision 3 | The check now normalises whitespace on both sides before comparing, so it asserts the words in order rather than the line width of a paragraph. `AGENTS.md` is unchanged and the required text is not weakened: reflowing a paragraph passes, deleting or rewording the assertion still fails. `node scripts/check-launch-mode.mjs` now reports `Launch Mode governance check passed.` |

## Rules

- Use sequential IDs: `PL-001`, `PL-002`, and so on.
- Do not delete deferred work because it is inconvenient or old; close it with evidence.
- A GitHub issue does not replace this index.
- When a ticket is promoted to a Launch Blocker, record the owner decision and move it to `LAUNCH-BLOCKERS.md`.
- Architecture, cleanup, refactoring, additional guards, hypothetical hardening and future capability default here unless they meet the blocker definition.
